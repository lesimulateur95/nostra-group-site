import { updateMaintenancePoleV153 } from "@/app/actions/v153";
import { DashboardHeader } from "@/components/dashboard/dashboard-header";
import { DashboardShell } from "@/components/dashboard/dashboard-shell";
import { getMaintenancePolesV153 } from "@/lib/v153/data";
import styles from "@/components/v153/v153.module.css";
export const dynamic="force-dynamic";
const labels:Record<string,string>={motors:"Nostra Motors",circuit:"Nostra Circuit",cercle:"Nostra Cercle",academy:"Racing Academy",events:"Événements & Jeux"};
export default async function MaintenancePage({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const [poles,p]=await Promise.all([getMaintenancePolesV153(),searchParams]);return <DashboardShell><DashboardHeader title="Maintenance par pôle" description="Fermer un seul service sans mettre tout Nostra Group hors ligne. Les gérants gardent l’accès pour tester." />{p.saved&&<p className={styles.feedback}>État du pôle enregistré.</p>}{p.error&&<p className={`${styles.feedback} ${styles.error}`}>Impossible d’enregistrer.</p>}<section className={styles.grid2}>{poles.map(pole=><article className={styles.card} key={pole.poleKey}><div className={styles.row}><div><p className={styles.eyebrow}>{labels[pole.poleKey]??pole.poleKey}</p><h2>{pole.enabled?"Maintenance active":"Service ouvert"}</h2></div><span className={styles.pill}>{pole.enabled?"FERMÉ AU PUBLIC":"OUVERT"}</span></div><form action={updateMaintenancePoleV153} className={styles.form}><input type="hidden" name="pole_key" value={pole.poleKey}/><label className={styles.full}>Titre<input name="title" defaultValue={pole.title}/></label><label className={styles.full}>Message<textarea name="message" rows={4} defaultValue={pole.message}/></label><label>Retour estimé<input name="eta_text" defaultValue={pole.etaText??""} placeholder="Ex. ce soir à 22 h"/></label><label><span>Activer la maintenance</span><input name="enabled" type="checkbox" defaultChecked={pole.enabled}/></label><div><button className={styles.button} type="submit">Enregistrer ce pôle</button></div></form></article>)}</section></DashboardShell>}
