import Link from "next/link"; import {DashboardShell} from "@/components/dashboard/dashboard-shell"; import {reviewRecruitment} from "@/app/actions/operations-v50"; import {recruitments} from "@/lib/operations-v50/data"; import styles from "@/components/operations-v50/operations.module.css";
export const dynamic="force-dynamic"; export default async function Page({searchParams}:{searchParams:Promise<{success?:string;error?:string}>}){const [a,q]=await Promise.all([recruitments(),searchParams]);return <DashboardShell><main className={styles.page}><section className={styles.hero}><Link className={styles.backLink} href="/dashboard">← Retour au Dashboard</Link><span>DIRECTION · RECRUTEMENT</span><h1>Candidatures</h1><p>Examiner, accepter, refuser et attribuer un rôle.</p></section>{q.success&&<div className={styles.success}>Décision enregistrée.</div>}{q.error&&<div className={styles.error}>Action impossible.</div>}<section className={styles.section}>{a.map(x=><article className={styles.card} key={x.id}><span>{x.application_number}</span><h3>{x.applicant_name} · {x.desired_position}</h3><div className={styles.gridTwo}><div><strong>Disponibilités</strong><p>{x.availability}</p></div><div><strong>Expérience</strong><p>{x.experience}</p></div></div><strong>Motivation</strong><p>{x.motivation}</p>{x.attachment_name&&<p>Pièce jointe : {x.attachment_name}</p>}<form className={styles.form} action={reviewRecruitment}><input type="hidden" name="id" value={x.id}/><label><span>Décision</span><select name="status" defaultValue={x.status}>{['received','reviewing','interview','accepted','rejected'].map(v=><option key={v} value={v}>{v}</option>)}</select></label><label><span>Rôle si acceptée</span><select name="role" defaultValue={x.role_to_assign??''}><option value="">Aucun</option><option value="employee">Employé</option><option value="commercial">Commercial</option><option value="commissioner">Commissaire</option></select></label><label><span>Message au candidat</span><textarea name="message" defaultValue={x.manager_message??''}/></label><button className={styles.primary}>Enregistrer</button></form></article>)}</section></main></DashboardShell>}
