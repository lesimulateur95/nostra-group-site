import { DashboardShell } from "@/components/dashboard/dashboard-shell";
import { DashboardHeader } from "@/components/dashboard/dashboard-header";
import { getStockOverviewV155 } from "@/lib/v155/data";
import { saveStockStateV155 } from "@/app/actions/v155";
import styles from "@/components/v155/v155.module.css";
export default async function StockRealPage(){const rows=await getStockOverviewV155();return <DashboardShell allowedRoles={["manager","employee","commercial"]}><DashboardHeader title="Stock réel Nostra Motors" description="Stock catalogue, unités réservées, locations actives, disponibilité physique et localisation."/><main className={styles.page} style={{paddingTop:10}}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Véhicule</th><th>Total</th><th>Réservé</th><th>Loué</th><th>Disponible</th><th>État opérationnel</th><th>Localisation</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.vehicleId}><td><strong>{r.brand} {r.model}</strong><br/><small>{r.catalogType} {r.showroom?"· showroom":""}</small></td><td>{r.totalStock}</td><td>{r.reserved}</td><td>{r.rented}</td><td className={r.available<=r.minimumStock?styles.warn:styles.good}>{r.available}</td><td colSpan={2}><form action={saveStockStateV155} className={styles.formGrid}><input type="hidden" name="vehicle_id" value={r.vehicleId}/><select className={styles.select} name="operational_status" defaultValue={r.status}>{["available","reserved","rented","workshop","unavailable"].map(x=><option key={x}>{x}</option>)}</select><input className={styles.input} name="physical_location" defaultValue={r.location}/><input className={styles.input} type="number" name="minimum_stock" defaultValue={r.minimumStock}/><input className={styles.input} name="notes" defaultValue={r.notes??""} placeholder="Note interne"/><button className={styles.button}>Enregistrer</button></form></td></tr>)}</tbody></table></div></main></DashboardShell>}
