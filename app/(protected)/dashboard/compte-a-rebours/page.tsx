import { DashboardHeader } from "@/components/dashboard/dashboard-header";
import { DashboardShell } from "@/components/dashboard/dashboard-shell";
import { saveGlobalCountdownV156 } from "@/app/actions/v156";
import { getGlobalCountdownV156 } from "@/lib/v156/data";
import styles from "@/components/v156/v156.module.css";
function dateInput(value:string|null|undefined){if(!value)return"";const d=new Date(value);if(Number.isNaN(d.getTime()))return"";const local=new Date(d.getTime()-d.getTimezoneOffset()*60000);return local.toISOString().slice(0,16)}
export default async function CountdownDashboard(){const c=await getGlobalCountdownV156();return <DashboardShell allowedRoles={["manager"]}><DashboardHeader title="Compte à rebours global" description="Afficher un compte à rebours visible sur l’ensemble du site jusqu’à une nouveauté ou un événement."/><main className={styles.page} style={{paddingTop:10}}><section className={styles.card}><form action={saveGlobalCountdownV156} className={styles.formGrid}><label className={styles.full}><input type="checkbox" name="enabled" defaultChecked={c?.enabled}/> Afficher le compte à rebours sur le site</label><label>Titre<input className={styles.input} name="title" defaultValue={c?.title??"Prochain rendez-vous Nostra"}/></label><label>Date / heure de fin<input className={styles.input} name="ends_at" type="datetime-local" defaultValue={dateInput(c?.endsAt)}/></label><label className={styles.full}>Sous-titre<input className={styles.input} name="subtitle" defaultValue={c?.subtitle??""}/></label><label className={styles.full}>Lien facultatif<input className={styles.input} name="target_url" defaultValue={c?.targetUrl??""} placeholder="/evenements/mystere"/></label><button className={`${styles.button} ${styles.full}`}>Enregistrer</button></form></section></main></DashboardShell>}
