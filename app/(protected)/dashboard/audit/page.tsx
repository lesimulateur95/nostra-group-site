import { DashboardShell } from "@/components/dashboard/dashboard-shell";
import { DashboardHeader } from "@/components/dashboard/dashboard-header";
import { getAuditV155 } from "@/lib/v155/data";
import styles from "@/components/v155/v155.module.css";
export default async function AuditPage({searchParams}:{searchParams:Promise<{q?:string}>}){const p=await searchParams;const rows=await getAuditV155(p.q??"");return <DashboardShell><DashboardHeader title="Journal d’audit" description="Traçabilité des modifications sensibles de Nostra Group."/><main className={styles.page} style={{paddingTop:10}}><form className={styles.actions}><input className={styles.input} name="q" defaultValue={p.q??""} placeholder="Rechercher action, table, identifiant…"/><button className={styles.button}>Rechercher</button></form><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Date</th><th>Action</th><th>Élément</th><th>Résumé</th><th>Acteur</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td>{new Date(r.created_at).toLocaleString("fr-FR")}</td><td><span className={styles.pill}>{r.action}</span></td><td>{r.entity_type}<br/><small>{r.entity_id}</small></td><td>{r.summary}</td><td>{r.actor_user_id??"Système"}</td></tr>)}</tbody></table></div></main></DashboardShell>}
