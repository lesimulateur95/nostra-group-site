import {DashboardShell} from "@/components/dashboard/dashboard-shell"; import {auditLogs} from "@/lib/operations-v50/data"; import styles from "@/components/operations-v50/operations.module.css";
export const dynamic="force-dynamic"; export default async function Page({searchParams}:{searchParams:Promise<{q?:string;entity?:string}>}){const p=await searchParams,l=await auditLogs(p.q??'',p.entity??'');return <DashboardShell><main className={styles.page}><section className={styles.hero}><span>DIRECTION · TRAÇABILITÉ</span><h1>Journal complet des actions</h1><p>Auteur, date, catégorie et élément concerné.</p></section><form className={styles.search} method="get"><label><span>Recherche</span><input name="q" defaultValue={p.q??''}/></label><label><span>Catégorie</span><select name="entity" defaultValue={p.entity??''}><option value="">Toutes</option>{['orders','invoices','pilot_license_applications','loyalty_memberships','plate_orders','motors_appointments','motors_sav_tickets','recruitment_applications'].map(x=><option key={x} value={x}>{x}</option>)}</select></label><button>Filtrer</button></form><section className={styles.section}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Date</th><th>Auteur</th><th>Action</th><th>Catégorie</th><th>Élément</th><th>Résumé</th></tr></thead><tbody>{l.map(x=><tr key={String(x.id)}><td>{new Date(String(x.created_at)).toLocaleString('fr-FR')}</td><td>{String(x.actor_name??'Système')}</td><td>{String(x.action_key)}</td><td>{String(x.entity_type)}</td><td>{String(x.entity_id??'—')}</td><td>{String(x.summary)}</td></tr>)}</tbody></table></div>{l.length===0&&<div className={styles.empty}>Aucune action.</div>}</section></main></DashboardShell>}
