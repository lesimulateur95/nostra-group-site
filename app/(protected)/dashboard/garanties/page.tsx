import { saveWarrantyPlanV163, updateWarrantyStatusV163 } from "@/app/actions/v163";
import { DashboardShell } from "@/components/dashboard/dashboard-shell";
import styles from "@/components/v163/v163.module.css";
import { getWarrantyAdminV163 } from "@/lib/v163/data";
export const dynamic="force-dynamic";
const money=(v:unknown)=>Number(v??0).toLocaleString("fr-FR",{style:"currency",currency:"EUR",maximumFractionDigits:0});
const date=(v:unknown)=>v?new Date(String(v)).toLocaleDateString("fr-FR"):"—";
export default async function WarrantyAdmin(){const data=await getWarrantyAdminV163();const vehicles=new Map(data.vehicles.map((x:any)=>[Number(x.id),x]));return <DashboardShell allowedRoles={["manager","employee","commercial"]}><div className={styles.page}><section className={styles.hero}><p className={styles.eyebrow}>NOSTRA CARE</p><h1>Contrats de garantie</h1><p className={styles.muted}>Crée des garanties Nostra avec durée en jours, prix, franchise et éléments couverts. Les contrats restent liés au véhicule du garage.</p></section>{!data.configured&&<div className={styles.error}>Exécute le SQL V163.</div>}<section className={styles.card}><p className={styles.eyebrow}>NOUVELLE GARANTIE</p><form action={saveWarrantyPlanV163} className={styles.form}><label>Nom<input name="name" required placeholder="Nostra Care+"/></label><label>Durée en jours<input name="duration_days" type="number" min="1" required defaultValue="180"/></label><label>Prix<input name="price" type="number" min="0" defaultValue="0"/></label><label>Franchise<input name="deductible" type="number" min="0" defaultValue="0"/></label><label className={styles.wide}>Description<textarea name="description"/></label>{[["engine","Moteur"],["gearbox","Boîte"],["electronics","Électronique"],["suspension","Suspension"],["bodywork","Carrosserie"],["tyres","Pneus"]].map(([n,l])=><label key={n}>{l}<input name={n} type="checkbox" defaultChecked={["engine","gearbox","electronics","suspension"].includes(n)}/></label>)}<label>Active<input name="active" type="checkbox" defaultChecked/></label><button className={`${styles.button} ${styles.wide}`}>Créer la formule</button></form></section><section className={styles.grid}>{data.plans.map((x:any)=><article className={styles.card} key={x.id}><p className={styles.eyebrow}>{x.duration_days} JOURS</p><h2>{x.name}</h2><p>{x.description}</p><div className={styles.row}><strong>{money(x.price)}</strong><span className={styles.pill}>Franchise {money(x.deductible)}</span></div><div className={styles.coverage}>{x.engine&&<span>Moteur</span>}{x.gearbox&&<span>Boîte</span>}{x.electronics&&<span>Électronique</span>}{x.suspension&&<span>Suspension</span>}{x.bodywork&&<span>Carrosserie</span>}{x.tyres&&<span>Pneus</span>}</div></article>)}</section><section className={styles.card}><h2>Contrats clients</h2><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Contrat</th><th>Véhicule</th><th>Formule</th><th>Prix</th><th>Expiration</th><th>Statut</th></tr></thead><tbody>{data.contracts.map((x:any)=>{const v:any=vehicles.get(Number(x.customer_vehicle_id));return <tr key={x.id}><td>{x.contract_number}</td><td>{v?`${v.brand??""} ${v.model??""}`.trim()||v.vehicle_name:`VL #${x.customer_vehicle_id}`}</td><td>{x.plan_name}<div className={styles.mini}>{x.duration_days} jours</div></td><td>{money(x.amount)}</td><td>{date(x.ends_at)}</td><td><form action={updateWarrantyStatusV163} className={styles.row}><input type="hidden" name="id" value={x.id}/><select name="status" defaultValue={x.status}><option value="pending_payment">Paiement attendu</option><option value="active">Actif</option><option value="expired">Expiré</option><option value="cancelled">Annulé</option></select><button className={styles.buttonAlt}>OK</button></form></td></tr>})}</tbody></table></div></section></div></DashboardShell>}
