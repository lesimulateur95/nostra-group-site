import { getNewsV155 } from "@/lib/v155/data";
import styles from "@/components/v155/v155.module.css";
export default async function NewsPage(){const rows=await getNewsV155(false);return <main className={styles.page}><section className={styles.hero}><span className={styles.eyebrow}>NOSTRA GROUP</span><h1>Actualités</h1><p>Nouveautés, ouvertures, annonces et temps forts de Nostra Motors, Circuit, Cercle et Academy.</p></section>{!rows.length?<div className={styles.empty}>Aucune actualité publiée.</div>:<div className={styles.newsGrid}>{rows.map((r:any)=><article className={styles.card} key={r.id}>{r.image_url?<div className={styles.media}><img src={r.image_url} alt={r.title}/></div>:null}<span className={styles.eyebrow}>{r.pole}</span><h2>{r.title}</h2><p>{r.excerpt||r.content}</p><small>{new Date(r.created_at).toLocaleDateString("fr-FR")}</small></article>)}</div>}</main>}
