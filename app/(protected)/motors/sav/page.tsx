import Link from "next/link"; import {redirect} from "next/navigation"; import {createSav} from "@/app/actions/operations-v50"; import {savTickets} from "@/lib/operations-v50/data"; import {createClient} from "@/lib/supabase/server"; import styles from "@/components/operations-v50/operations.module.css";
export const dynamic="force-dynamic"; export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const s=await createClient(),{data}=await s.auth.getUser();if(!data.user)redirect('/');const [tickets,q]=await Promise.all([savTickets(data.user.id),searchParams]);return <main className={styles.page}><section className={styles.hero}><span>NOSTRA MOTORS · ASSISTANCE</span><h1>SAV Nostra Motors</h1><p>Ouvrez un dossier et échangez avec l’équipe.</p></section>{q.error&&<div className={styles.error}>{q.error==='file-size'?'Fichier supérieur à 8 Mo.':q.error==='file-type'?'PDF, JPG ou PNG uniquement.':'Le dossier n’a pas pu être créé.'}</div>}<section className={styles.gridTwo}><form className={styles.form} action={createSav}><label><span>Catégorie</span><select name="category" defaultValue="vehicle"><option value="vehicle">Véhicule</option><option value="order">Commande</option><option value="delivery">Livraison</option><option value="plate">Plaque</option><option value="payment">Paiement</option><option value="other">Autre</option></select></label><label><span>Priorité</span><select name="priority" defaultValue="normal"><option value="normal">Normale</option><option value="urgent">Urgente</option><option value="critical">Critique</option></select></label><label><span>Sujet</span><input name="subject" required maxLength={180}/></label><label><span>Référence véhicule ou commande</span><input name="reference" maxLength={180}/></label><label><span>Description</span><textarea name="description" required minLength={10}/></label><label><span>Pièce jointe (PDF/JPG/PNG, 8 Mo)</span><input name="attachment" type="file" accept=".pdf,.jpg,.jpeg,.png"/></label><button className={styles.primary}>Envoyer au SAV</button></form><section className={styles.section}><header><span>MES DOSSIERS</span><h2>{tickets.length} dossier(s)</h2></header><div className={styles.list}>{tickets.length===0&&<div className={styles.empty}>Aucun dossier.</div>}{tickets.map(t=><article className={styles.row} key={t.id}><div><h3>{t.ticket_number} · {t.subject}</h3><p>{t.status} · {t.priority}</p></div><Link className={styles.actionLink} href={`/motors/sav/${t.id}`}>Ouvrir →</Link></article>)}</div></section></section></main>}
