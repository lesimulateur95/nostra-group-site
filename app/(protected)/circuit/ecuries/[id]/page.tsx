import { notFound } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import { getTeamProfileV153 } from "@/lib/v153/data";
import styles from "@/components/v153/v153.module.css";
export const dynamic="force-dynamic";
export default async function TeamPublicPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const teamId=Number(id);if(!Number.isFinite(teamId))notFound();const supabase=await createClient();const [{data:team},profile]=await Promise.all([(supabase as any).from("team_registration_requests").select("*").eq("id",teamId).eq("status","approved").maybeSingle(),getTeamProfileV153(teamId)]);if(!team||!profile?.publicVisible)notFound();return <main className={styles.page}><section className={styles.heroTeam} style={{["--team-primary" as string]:profile.primaryColor}}><div className={styles.row}><div>{profile.logoUrl&&<img className={styles.logo} src={profile.logoUrl} alt=""/>}<p className={styles.eyebrow}>ÉCURIE OFFICIELLE · {String(team.registration_type).toUpperCase()}</p><h1>{team.team_name}</h1><p>{profile.slogan}</p></div><span className={styles.pill}>VALIDÉE NOSTRA CIRCUIT</span></div></section><section className={styles.grid2} style={{marginTop:18}}><article className={styles.card}><h2>Présentation</h2><p>{profile.description||"Présentation à venir."}</p><p><strong>Base :</strong> {profile.headquarters||"Non renseignée"}</p><p><strong>Véhicule :</strong> {profile.vehicleModel||"Non renseigné"}</p></article><article className={styles.card}><h2>Pilotes</h2><div className={styles.stack}><div><span className={styles.muted}>Pilote principal</span><h3>{profile.principalDriver||"À annoncer"}</h3></div><div><span className={styles.muted}>Deuxième pilote</span><h3>{profile.secondDriver||"À annoncer"}</h3></div><div><span className={styles.muted}>Réserve</span><h3>{profile.reserveDriver||"À annoncer"}</h3></div></div></article><article className={styles.card}><h2>Palmarès</h2><p style={{whiteSpace:"pre-line"}}>{profile.achievements||"Aucun résultat publié pour le moment."}</p></article><article className={styles.card}><h2>Partenaires</h2><p style={{whiteSpace:"pre-line"}}>{profile.sponsors||"Aucun partenaire publié."}</p></article></section></main>}
